console.log("Test: Array without label");
const arr = [10, 20, 30];
const a = arr[0];
const b = arr[1];
console.log(a);  // No label, just value
console.log(b);  // No label, just value
console.log("Done");
