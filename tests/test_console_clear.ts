function main(): number {
    // console.clear() - clears the console
    // In browsers, this clears the console window
    // In terminals, can use ANSI escape codes to clear screen

    console.log("Before clear");
    console.clear();
    console.log("After clear");

    // Return success code
    return 165;
}
