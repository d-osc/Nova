// Simple ternary operator test
function main(): number {
    let x = 10;
    let y = 20;

    // Simple ternary: return larger value
    let result = x > y ? x : y;  // Should be 20

    return result;
}
