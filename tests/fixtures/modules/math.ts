export const base: number = 40;
export const label: string = "nova";
export const enabled: boolean = true;
export default 7;

export function add(left: number, right: number): number {
    return left + right;
}
