// Test template literals with numeric expressions

const count = 42;
const port = 3000;

console.log(`Server handled ${count} requests successfully!`);
console.log(`Listening on port ${port}`);
console.log(`Result: ${count + 8} total`);
