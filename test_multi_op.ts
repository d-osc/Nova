// Test case: Multiple arithmetic operations
function multiOp(a: number, b: number, c: number): number {
    const add = a + b;
    const sub = add - c;
    const mul = sub * 2;
    const div = mul / 2;
    return div;
}
