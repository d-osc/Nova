// Test basic array literal syntax

function testArrayLiteral(): number {
    // Create array literal
    let arr = [1, 2, 3];
    
    // For now, just return the array size
    return 3;
}

function main(): number {
    return testArrayLiteral();
}