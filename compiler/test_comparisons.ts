function compareNumbers(): number {
    const a = 10;
    const b = 5;
    
    if (a > b) {
        return 1;
    } else {
        return 0;
    }
}

function main(): number {
    return compareNumbers();
}