// Test array.join() method  
function main(): number {
    let arr = [1, 2, 3];
    let result = arr.join(",");
    
    // For now, just check that join exists and returns something
    // Full string comparison would need string equality
    return 1;  // Placeholder - just test compilation
}
