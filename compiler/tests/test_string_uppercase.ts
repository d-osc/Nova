// Test string toUpperCase method
function main(): number {
    let str = "hello";
    let upper = str.toUpperCase();

    // Since we can't compare strings easily, just test compilation
    // Return 1 if it compiles successfully
    return 1;
}
