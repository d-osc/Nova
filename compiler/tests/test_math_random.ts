// Test Math.random() function
function main(): number {
    // Math.random() returns a pseudo-random number
    // For integer type system, returns pseudo-random integer
    // We'll just test that it executes without error
    let a = Math.random();
    let b = Math.random();
    let c = Math.random();

    // Since random values are unpredictable, just verify it runs
    // Return 1 to indicate success
    return 1;
}
