// Test basic Promise creation and then()

function main(): number {
    console.log("Creating Promise...");

    let p = new Promise();
    console.log("Promise created");

    // This is a basic test - just verify Promise can be created
    console.log("Test passed!");
    return 0;
}
