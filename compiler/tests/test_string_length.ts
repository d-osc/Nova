// String length property test
function testStringLength(): number {
    let s = "Hello";
    let len = s.length;  // Should be 5
    return len;
}

function main(): number {
    return testStringLength();
}
