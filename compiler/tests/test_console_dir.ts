function main(): number {
    // console.dir() - displays object/value properties in readable format
    // Used for inspecting values and data structures

    console.log("Testing console.dir()");

    // Test with number
    let num = 42;
    console.dir(num);

    // Test with string
    let str = "Hello Nova";
    console.dir(str);

    // Test with array
    let arr = [1, 2, 3];
    console.dir(arr);

    console.log("dir() test complete");

    // Return success code
    return 184;
}
