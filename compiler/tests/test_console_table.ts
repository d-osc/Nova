function main(): number {
    // console.table(data) - displays data in tabular format
    // Used for visualizing arrays

    // Test with array of numbers
    let numbers = [10, 20, 30];
    console.table(numbers);

    // Return success code
    return 181;
}
