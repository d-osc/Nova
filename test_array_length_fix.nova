// Test case for array.length bug fix
const arr = [];
for (let i = 0; i < 3; i++) {
    arr.push(i);
}
console.log("Array length: " + arr.length);

// Additional test cases
const arr2 = [1, 2, 3];
console.log("arr2.length: " + arr2.length);

const arr3 = [10, 20, 30, 40, 50];
console.log("arr3.length: " + arr3.length);

// Test after push
arr3.push(60);
console.log("arr3.length after push: " + arr3.length);
